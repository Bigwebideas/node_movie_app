<% include ./includes/header.ejs %>

<div class="movie_container">
    <div class="container">
        <div class="row">
            <% for(var i=0; i < list_tv_shows.length; i++) { %>
                <div class="col image_wrap s3">
                    <img src="<%= poster_path %><%= list_tv_shows[i].poster_path %>" class="img-responsive" alt="<%= list_tv_shows[i].name %>" />
                </div>
            <% } %>
        </div>
    </div>
</div>

<% include ./includes/footer.ejs %>